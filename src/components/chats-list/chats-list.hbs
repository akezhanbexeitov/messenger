<ul class="chats-list">
    {{#each chats}}
        {{{ 
            ChatsCard 
                avatar=this.avatar
                id=this.id
                lastMessage=this.lastMessage
                title=this.title
                unreadCount=this.unreadCount
        }}}
    {{/each}}
</ul>
