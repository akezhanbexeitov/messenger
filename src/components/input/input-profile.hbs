<input 
    class="field-profile__line__input" 
    type={{ type }} 
    id={{ name }} 
    placeholder={{ placeholder }} 
    name={{ name }}
    ref="input" 
    {{#if disabled }} disabled {{/if}}
>
