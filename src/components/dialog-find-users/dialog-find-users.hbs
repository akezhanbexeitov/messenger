{{#Dialog open=isOpenDialogUsers}}
  <form method="dialog">
    <h3>Find user</h3>

    {{{
      Field
        name="chatTitle"
        label="Find user"
        placeholder="User"
        type="text"
        ref="user"
        env="chat"
    }}}

    {{{ ErrorText error=error ref="errorLine" type="form" }}}

    <div>
      {{{
        Button
          text="Create"
          primary="true"
          type="submit"
          onClick=onSave
      }}}
      {{{
        Button
          text="Cancel"
          type="button"
          onClick=onClose
      }}}
    </div>
  </form>
{{/Dialog}}
